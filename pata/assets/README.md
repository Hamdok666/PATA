This folder is for static assets used by the `pata` demo site.

- Optional: add `africa.jpg` (photo) to be used as the hero background referenced by `style.css`.
- If you don't have an image, remove the `background-image` rule in `pata/style.css` or provide a small placeholder image named `africa.jpg`.

Place any images here and add them to `.gitignore` if they are large or proprietary.
